# ===========================================
# Docker Log Viewer - Configuration
# ===========================================
# Copy this file to .env and customize values
# cp .env.example .env

# ===========================================
# Security Settings
# ===========================================

# JWT Secret key - MUST change in production!
# Generate a secure key: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ===========================================
# Server Settings
# ===========================================

# Backend port (internal, exposed to nginx)
# BACKEND_PORT=2001

# Frontend port (external, accessible from browser)
FRONTEND_PORT=2000

# Node environment (production/development)
NODE_ENV=production

# ===========================================
# Default Admin Credentials
# ===========================================
# First login credentials (change password after login!)
# Username: admin
# Password: admin123

# ===========================================
# Docker Settings
# ===========================================

# Docker socket path (default: /var/run/docker.sock)
# DOCKER_SOCKET=/var/run/docker.sock
